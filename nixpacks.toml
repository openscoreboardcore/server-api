[phases.setup]
nixPkgs = [
  "bun",
  "vips",   # <-- This provides libvips + libvips-cpp.so.42
  "gcc",    # optional but recommended
  "glibc"   # optional but recommended
]

[phases.install]
cmds = ["bun install"]

[start]
cmd = "bun run src/index.ts"